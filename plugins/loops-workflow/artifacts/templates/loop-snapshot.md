# loop：<slug>

> 由 `scripts/loop-snapshot.mjs` 自 `events.jsonl` 重生（deterministic）。手改會被下次重生蓋掉——要改狀態就 append 事件。
> 這份模板記錄的是**契約**：哪些區塊一定會有、哪些只在有內容時才出現。renderer 是唯一產生者。

| 欄位 | 值 |
|---|---|
| 類型 | … |
| operation | … |
| 推進模式 | … |
| 當前階段 | … |
| 回環 | … |
| 停止條件 | … |
| session | … |
| issue | … |
| 事件數 | … |

## 仍擋著完工的

一律出現。沒有東西擋著時寫「（無）」——不是省略這一段。
列的是 iterate §6 的完工前提（未修 findings／沒過的閘／未決決策／未解決的 blocking unknown），不是 pr-gate 閘⑥ 的機械下界。

## 未完成任務

**條件式**：只有還有未完成任務時才出現。

## 最近事件（最多 N 筆，非完整 Journal）

一律出現。標題帶動態上限數字，所以契約比對是**前綴比對**、不是全等。
沒有事件時寫「（無事件）」。
