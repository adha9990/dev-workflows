# loop: 134-lessons-baked（摘錄／已重排格式，非逐字複製）

> 方法論見對應 corpus JSON 的 provenance.method——本檔把真實歷史 Journal 的散文內容，
> 重排成 `eval-trajectory.mjs` 的 `parseStages` 實際辨識的 `- E# [stagename] ...` 格式
> （真實 loop.md 用的是 `[E1]`/`[E2]` 數字編號在前，stage 名不在方括號內，
> `parseStages` 不會把它解成 stage——這是本次建置發現的真實格式落差，
> 已回報 team-lead）。每行仍對應真實 Journal 條目與真實內容摘要，只有標記格式改變。

## Journal

- E1 [goal] docs-only 票起手，六則落點/句數上限/AC 全寫死，goal 零訪談直取
- E2 [plan] 審查 r1，2 項要修（皆實錘）
- E3 [plan] r2 複審，D6/T2 收乾淨
- E4 [plan] r3，收斂 2→2→1，性質結構→機械，定稿派 impl
- E5 [build] impl-134 逐字執行 15 處/8 檔，零偏離
- E6 [verify] 首輪，contract=Ready，docs-devex=Not ready（P1+P2+P3×2）
- E7 [iterate] delta 複驗回環第 2 圈，原 4 條全收乾淨，新抓 1 P2
- E8 [iterate] delta2=Ready，acceptance 閘過，PR #148 merge
